# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: timesheetodm
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: 0M6GQR9_SAML_ASSERTION
          url: https://my420983.s4hana.cloud.sap
          authenticationType: reentranceTicket # SAML support for vscode
        app:
          name: ZODMSAISIE
          description: saisie des temps
          package: Z_APP_ODM_RAP
          transport: P3BK900321
        exclude:
          - /test/
